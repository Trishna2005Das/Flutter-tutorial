import 'package:flutter/material.dart';
class CurrencyConverter extends satelessWidget{
  const CurrencyConverter({super.key});
  @override
  Widget build(BuildContext context) {
    Scaffold(
        body: Center(
          child: Text(
            'Hello World',
         ) ))}


}